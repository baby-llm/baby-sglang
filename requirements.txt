# Core dependencies for baby-sglang MVP
torch>=2.0.0
transformers>=4.30.0
huggingface_hub>=0.16.0
dataclasses>=0.6; python_version < "3.7"

# Optional dependencies for advanced features (not required for MVP)
vllm>=0.4.0  # For benchmarking and advanced attention kernels
# zmq>=0.0.0   # For multi-process communication (future)
# uvloop>=0.0.0 # For async performance (future)